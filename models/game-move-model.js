//imports built in

//inports 3rd party

//imports custom

class GameMove {
  constructor(playerNumber, coord, date, roomId) {
    this.playerNumber = playerNumber;
    this.coord = coord; //[row, col] array
    this.date = date;
    this.roomId = roomId;
  }
  //
}

//export
module.exports = GameMove;
